import PermissionHandler from './Components/permissionHandler';

//Renders a single components, which can then render further components
function App() {
  return (
    <div>
      <PermissionHandler></PermissionHandler>
    </div>
  );
}

export default App;
